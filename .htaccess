<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -l
  RewriteRule ^ - [L]
  RewriteRule ^api/(.*)$ api/route.php?action=$1 [QSA,L]
  RewriteRule ^(.*)/(.*)$ route.php?action=$1 [QSA,L]
  RewriteRule ^(.*)$ route.php?action=$1 [QSA,L]
</IfModule>
